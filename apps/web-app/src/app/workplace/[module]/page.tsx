'use client';
import { useParams } from 'next/navigation';
import Link from 'next/link';
import { AppSidebar } from '@/components/AppSidebar';
const copy: Record<string, [string, string, string]> = { maintenance: ['Maintenance', 'Plan, assign, and complete preventive or corrective work.', 'Create maintenance task'], facilities: ['Facilities', 'Manage sites, rooms, desks, and workplace availability.', 'Book a resource'], visitors: ['Visitor management', 'Prepare hosts and securely coordinate office visitors.', 'Register visitor'], procurement: ['Procurement', 'Track purchases from request to approval and delivery.', 'Create purchase request'], fleet: ['Fleet', 'Coordinate fleet availability, assignments, and servicing.', 'Add vehicle'], compliance: ['Compliance', 'Run audits, collect evidence, and keep policies on track.', 'Schedule audit'], analytics: ['Operations analytics', 'Review trends across people, assets, and workplace operations.', 'Create report'] };
export default function WorkplaceModule() { const { module } = useParams<{module: string}>(); const [title, description, action] = copy[module] ?? ['Workplace module', 'This workplace capability is available through Nexora.', 'Create record']; return <div className="flex h-screen overflow-hidden bg-slate-950 text-slate-100"><AppSidebar /><main className="flex-1 overflow-y-auto p-6 sm:p-10"><Link href="/workplace" className="text-sm font-semibold text-emerald-300">← Workplace</Link><div className="mt-8 flex flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-emerald-400">Nexora workplace</p><h1 className="mt-2 text-4xl font-black">{title}</h1><p className="mt-3 max-w-xl text-slate-400">{description}</p></div><button className="rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold hover:bg-emerald-500">{action}</button></div><section className="glass-card mt-8 rounded-2xl p-8"><h2 className="font-bold">Ready for your operational data</h2><p className="mt-2 max-w-2xl text-sm leading-relaxed text-slate-400">This module is routed through the unified Nexora Workplace API. Once the shared database migration and HRMS import are applied, your existing records will appear here with the same access controls, audit history, and live notifications as the rest of Nexora.</p></section></main></div>; }
