# Suppress peer-dependency resolution errors caused by nested Clerk/NestJS version trees.
# Using --legacy-peer-deps here means npm resolves packages the npm v6 way, which is
# safe for this monorepo where all transitive peers are actually satisfied at runtime.
legacy-peer-deps=true
